from setuptools import setup
import re


setup(
    name="events_package",
    version="5.0",
    packages=["events_package"],
    author=["Pawel Mucha"],
    author_email=["pawel_mucha@zohomail.eu"],
)
